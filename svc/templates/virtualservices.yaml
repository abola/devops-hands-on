apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: {{ .Values.gateway.name }}
spec:
  selector:
    istio: ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
# Bookinfo UI
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Values.istio.bookinfo.name }}
spec:
  hosts:
{{-if .Values.istio.ingressgateway.ip }}
  - "{{ .Values.istio.bookinfo.name }}.{{ .Values.istio.ingressgateway.ip }}.nip.io"
{{-end }}
{{-if .Values.istio.bookinfo.dns }}
  - "{{ .Values.istio.bookinfo.dns }}"
{{-end }}  
  gateways:
  - {{ .Values.gateway.name }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: {{ .Values.istio.bookinfo.svc }}
        port:
          number: {{ .Values.istio.bookinfo.port }}
---
# Jaeger UI
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Values.istio.jaeger.name }}
spec:
  hosts:
{{-if .Values.istio.ingressgateway.ip }}
  - "{{ .Values.istio.jaeger.name }}.{{ .Values.istio.ingressgateway.ip }}.nip.io"
{{-end }}
{{-if .Values.istio.bookinfo.dns }}
  - "{{ .Values.istio.jaeger.dns }}"
{{-end }}    
  gateways:
  - {{ .Values.gateway.name }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: {{ .Values.istio.jaeger.svc }}
        port:
          number: {{ .Values.istio.jaeger.port }}
---
# Kiali UI
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Values.istio.kiali.name }}
spec:
  hosts:
{{-if .Values.istio.ingressgateway.ip }}
  - "{{ .Values.istio.kiali.name }}.{{ .Values.istio.ingressgateway.ip }}.nip.io"
{{-end }}
{{-if .Values.istio.kiali.dns }}
  - "{{ .Values.istio.kiali.dns }}"
{{-end }}     
  gateways:
  - {{ .Values.gateway.name }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: {{ .Values.istio.kiali.svc }}
        port:
          number: {{ .Values.istio.kiali.port }}
---
# Grafana UI
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Values.istio.grafana.name }}
spec:
  hosts:
{{-if .Values.istio.ingressgateway.ip }}
  - "{{ .Values.istio.grafana.name }}.{{ .Values.istio.ingressgateway.ip }}.nip.io"
{{-end }}
{{-if .Values.istio.grafana.dns }}
  - "{{ .Values.istio.grafana.dns }}"
{{-end }}    
  gateways:
  - {{ .Values.gateway.name }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: {{ .Values.istio.grafana.svc }}
        port:
          number: {{ .Values.istio.grafana.port }}
---
# Kibana UI
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Values.istio.kibana.name }}
spec:
  hosts:
{{-if .Values.istio.ingressgateway.ip }}
  - "{{ .Values.istio.kibana.name }}.{{ .Values.istio.ingressgateway.ip }}.nip.io"
{{-end }}
{{-if .Values.istio.kibana.dns }}
  - "{{ .Values.istio.kibana.dns }}"
{{-end }}      
  gateways:
  - {{ .Values.gateway.name }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: {{ .Values.istio.kibana.svc }}
        port:
          number: {{ .Values.istio.kibana.port }}
---
# Jenkins CI/CD
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Values.jenkins.name }}
spec:
  hosts:
{{-if .Values.istio.ingressgateway.ip }}
  - "{{ .Values.jenkins.name }}.{{ .Values.istio.ingressgateway.ip }}.nip.io"
{{-end }}
{{-if .Values.jenkins.dns }}
  - "{{ .Values.jenkins.dns }}"
{{-end }}       
  gateways:
  - {{ .Values.gateway.name }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: {{ .Values.jenkins.svc }}
        port:
          number: {{ .Values.jenkins.port }}
---
# KeyCloak
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Values.keycloak.name }}
spec:
  hosts:
{{-if .Values.istio.ingressgateway.ip }}
  - "{{ .Values.keycloak.name }}.{{ .Values.istio.ingressgateway.ip }}.nip.io"
{{-end }}
{{-if .Values.keycloak.dns }}
  - "{{ .Values.keycloak.dns }}"
{{-end }}     
  gateways:
  - {{ .Values.gateway.name }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: {{ .Values.keycloak.svc }}
        port:
          number: {{ .Values.keycloak.port }}
